
/**
 * First we will load all of this project's JavaScript dependencies which
 * includes Vue and other libraries. It is a great starting point when
 * building robust, powerful web applications using Vue and Laravel.
 */

require('./bootstrap');

// window.Vue = require('vue');

// Vue.component('example', require('./components/Example.vue'));

// const app = new Vue({
//     el: '#app'
// });

/**
 * Next, we will create a fresh Vue application instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */

require('./vendor/icheck');
require('./vendor/select2');
require('./vendor/bootstrap-datetimepicker');
require('./vendor/bootstrap-datetimepicker.zh-CN');
window.swal = require('./vendor/sweetalert2');
require('./vendor/sparkline');
require('./vendor/data-tables');
require('./vendor/uniform');
require('./vendor/collect');
require('./vendor/generic');
require('./vendor/odds');
